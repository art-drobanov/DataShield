namespace DataShield.Interfaces;

// ─────────────────────────────────────────────────────────────────────────────
//  Делегат вычитки буфера источника данных
// ─────────────────────────────────────────────────────────────────────────────

/// <summary>
/// Делегат на открытый метод вычитки данных из буфера источника.
/// </summary>
/// <returns>
/// Копия содержимого буфера. Длина может быть меньше <see cref="IDataSource.BufferSize"/>
/// у последнего частичного буфера (EOF или остановка источника).
/// Повторный вызов возвращает пустой массив.
/// </returns>
public delegate byte[] TakeBufferDelegate();
